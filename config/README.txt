Rebuilt backend sync bundle.

These files were reconstructed from stored versions in the user's file library plus the current app.py upload.
They are packaged as clean downloadable folders because the earlier artifact links were not accessible.
